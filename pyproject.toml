[project]
name = "fdl-nlftp"
version = "0.1.0"
description = "国土数値情報 dataset for fdl"
requires-python = ">=3.13"
dependencies = [
    "frozen-ducklake>=0.11.0",
    "dbt-core>=1.10.0",
    "dbt-duckdb>=1.10.0",
    "pypdf>=5.0.0",
]
